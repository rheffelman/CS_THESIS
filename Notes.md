What is the cleanest/best way to do function syntax?
some mix of rust and python? implicit arg types and return types
```
fn add(a, b) {
    return a + b;
}
```

How should custom objects/structs work?
